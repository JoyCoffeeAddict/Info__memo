import React from 'react';
import classes from './Footer.module.scss';

const Footer = props => (
	<div className={classes.footer}>
		<span>&copy; Bartłomiej Rzepecki</span>
	</div>
);

export default Footer;
